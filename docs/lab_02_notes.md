The only values I can say "changed" were the empty market entries now say 'Kampala', made this decision because you 
cannot have a transaction without a place where it happened but we could not reject those rows. I distinguished outliers 
from errors by looking at their trend of interpolation, outliers go in a certain direction and seem to start 
where this ends but errors just appear in the wrong place at the wrong time. Leakage would occur by accessing prediction
data from the raw folder rather than the processed or interim folders since they are in the same directory/module.